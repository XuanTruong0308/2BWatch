# SQL Server SA password must satisfy SQL Server complexity rules
# At least 8 chars, uppercase, lowercase, number, special char
MSSQL_SA_PASSWORD=StrongPassw0rd!234

# Optional: prebuilt app image tag for docker-compose.deploy.yml
APP_IMAGE=2bshop-web:1.0.0
